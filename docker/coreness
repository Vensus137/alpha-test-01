#!/bin/bash

# Глобальная команда для управления Coreness
# Использование: coreness [start|stop|restart|logs|status|update]

# Цвета для вывода
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m'

# Глобальные переменные для настройки команд
COMMAND_NAME="coreness"
COMMAND_DESCRIPTION="Coreness - Управление системой"

# Пути к скриптам и файлам
TOOLS_DIR="tools/core"
DATABASE_MANAGER_SCRIPT="tools/database_manager.py"
CORE_UPDATER_SCRIPT="tools/core/core_updater.py"
DOCKER_COMPOSE_FILE="docker-compose.yml"
DOCKER_DIR="docker"
MAIN_SCRIPT="main.py"

# Настройки контейнера
CONTAINER_NAME="coreness-service"
CONTAINER_STATS_NAME="coreness-container"

# Алиасы команд
ALIASES=("coreness-start" "coreness-stop" "coreness-restart" "coreness-logs" "coreness-status" "coreness-update" "coreness-db" "coreness-shell" "coreness-kill")

# Глобальная переменная для пути к проекту
PROJECT_DIR=""

# Функция для поиска папки проекта
find_project_dir() {
    # Проверяем переменную окружения
    if [ -n "$CORENESS_PROJECT_DIR" ] && [ -d "$CORENESS_PROJECT_DIR" ]; then
        PROJECT_DIR="$CORENESS_PROJECT_DIR"
        return 0
    fi
    
    # Ищем папку с docker compose.yml в текущей директории
    if [ -f "./$DOCKER_DIR/$DOCKER_COMPOSE_FILE" ]; then
        PROJECT_DIR="$(pwd)"
        return 0
    fi
    
    # Ищем в родительской директории
    if [ -f "../$DOCKER_DIR/$DOCKER_COMPOSE_FILE" ]; then
        PROJECT_DIR="$(dirname "$(pwd)")"
        return 0
    fi
    
    # Ищем в домашней директории
    for dir in "$HOME"/*; do
        if [ -d "$dir" ] && [ -f "$dir/$DOCKER_DIR/$DOCKER_COMPOSE_FILE" ]; then
            PROJECT_DIR="$dir"
            return 0
        fi
    done
    
    # Ищем в /opt
    if [ -d "/opt" ]; then
        for dir in /opt/*; do
            if [ -d "$dir" ] && [ -f "$dir/$DOCKER_DIR/$DOCKER_COMPOSE_FILE" ]; then
                PROJECT_DIR="$dir"
                return 0
            fi
        done
    fi
    
    return 1
}

# Функция для проверки Docker
check_docker() {
    if ! command -v docker &> /dev/null; then
        echo -e "${RED}❌ Docker не установлен!${NC}"
        exit 1
    fi
    
    if ! docker info &> /dev/null; then
        echo -e "${RED}❌ Docker не запущен!${NC}"
        exit 1
    fi
}

# Функция для проверки проекта и перехода в директорию
ensure_project_dir() {
    if ! find_project_dir; then
        echo -e "${RED}❌ Проект не найден!${NC}"
        exit 1
    fi
    cd "$PROJECT_DIR"
}

# Функция для выполнения docker compose команд
docker_compose() {
    docker compose -f docker/docker-compose.yml "$@"
}

# Функция для проверки и запуска контейнера если нужно
ensure_container_running() {
    if ! docker_compose ps | grep -q "$CONTAINER_STATS_NAME.*Up"; then
        echo -e "${YELLOW}⚠️ Контейнер не запущен, запускаю...${NC}"
        docker_compose up -d
    fi
}

# Функция для установки команд
install_commands() {
    echo -e "${BLUE}================================${NC}"
    echo -e "${BLUE}   УСТАНОВКА ГЛОБАЛЬНЫХ КОМАНД   ${NC}"
    echo -e "${BLUE}================================${NC}"
    
    # Проверяем права
    if [ "$EUID" -eq 0 ]; then
        echo -e "${YELLOW}⚠️ Запуск от root. Команды будут установлены глобально.${NC}"
        INSTALL_DIR="/usr/local/bin"
    else
        echo -e "${YELLOW}⚠️ Запуск от обычного пользователя. Команды будут установлены локально.${NC}"
        INSTALL_DIR="$HOME/.local/bin"
        
        # Создаем папку если не существует
        mkdir -p "$INSTALL_DIR"
        
        # Добавляем в PATH если не добавлен
        if ! echo "$PATH" | grep -q "$INSTALL_DIR"; then
            echo -e "${YELLOW}💡 Добавляем $INSTALL_DIR в PATH...${NC}"
            echo "export PATH=\"\$PATH:$INSTALL_DIR\"" >> "$HOME/.bashrc"
            echo "export PATH=\"\$PATH:$INSTALL_DIR\"" >> "$HOME/.profile"
            echo -e "${GREEN}✅ PATH обновлен. Перезапустите терминал или выполните: source ~/.bashrc${NC}"
        fi
    fi
    
    # Получаем путь к исходному скрипту
    # Если мы запущены из /usr/local/bin, ищем исходный скрипт в docker/
    if [ "$(dirname "${BASH_SOURCE[0]}")" = "/usr/local/bin" ]; then
        # Ищем проект и используем скрипт из docker/
        if find_project_dir; then
            SCRIPT_PATH="$PROJECT_DIR/docker/coreness"
        else
            echo -e "${RED}❌ Не удалось найти исходный скрипт coreness${NC}"
            exit 1
        fi
    else
        # Если запущены из docker/, используем текущий скрипт
        SCRIPT_PATH="${BASH_SOURCE[0]}"
    fi
    
    # Удаляем старую команду если существует
    if [ -f "$INSTALL_DIR/$COMMAND_NAME" ]; then
        echo -e "${YELLOW}🔄 Обновляю существующую команду...${NC}"
        rm "$INSTALL_DIR/$COMMAND_NAME"
    fi
    
    # Копируем основной скрипт
    if cp "$SCRIPT_PATH" "$INSTALL_DIR/$COMMAND_NAME"; then
        chmod +x "$INSTALL_DIR/$COMMAND_NAME"
        echo -e "${GREEN}✅ Команда '$COMMAND_NAME' установлена${NC}"
    else
        echo -e "${RED}❌ Ошибка установки команды '$COMMAND_NAME'${NC}"
        exit 1
    fi
    
    # Создаем алиасы
    create_aliases
    
    echo -e "${GREEN}🎉 Глобальные команды установлены!${NC}"
    echo ""
    echo -e "${BLUE}Теперь вы можете использовать:${NC}"
    echo "  $COMMAND_NAME install   # Установка команд"
    echo "  $COMMAND_NAME start     # Запуск"
    echo "  $COMMAND_NAME stop      # Остановка"
    echo "  $COMMAND_NAME restart   # Перезапуск"
    echo "  $COMMAND_NAME logs      # Логи"
    echo "  $COMMAND_NAME status    # Статус"
    echo "  $COMMAND_NAME update    # Обновление"
    echo "  $COMMAND_NAME db        # Работа с БД"
    echo "  $COMMAND_NAME shell     # Shell в контейнере"
    echo "  $COMMAND_NAME help      # Справка"
}

# Функция для создания алиасов
create_aliases() {
    echo -e "${YELLOW}🔗 Создание алиасов...${NC}"
    
    # Создаем алиасы в .bashrc
    ALIAS_FILE="$HOME/.bashrc"
    
    # Проверяем, есть ли уже алиасы
    if ! grep -q "# $COMMAND_DESCRIPTION aliases" "$ALIAS_FILE" 2>/dev/null; then
        echo "" >> "$ALIAS_FILE"
        echo "# $COMMAND_DESCRIPTION aliases" >> "$ALIAS_FILE"
        
        # Создаем алиасы из массива
        for alias in "${ALIASES[@]}"; do
            # Извлекаем команду из алиаса (убираем префикс)
            command="${alias#$COMMAND_NAME-}"
            echo "alias $alias='$COMMAND_NAME $command'" >> "$ALIAS_FILE"
        done
        
        echo -e "${GREEN}✅ Алиасы созданы${NC}"
    else
        echo -e "${GREEN}✅ Алиасы уже существуют${NC}"
    fi
}

# Универсальная функция для запуска контейнера
start_service() {
    echo -e "${YELLOW}🚀 Запуск контейнера...${NC}"
    
    # Запускаем контейнер напрямую по имени
    if docker start $CONTAINER_STATS_NAME; then
        echo -e "${GREEN}✅ Контейнер запущен${NC}"
        echo -e "${YELLOW}💡 Для запуска приложения используйте: $COMMAND_NAME app-start${NC}"
    else
        echo -e "${RED}❌ Ошибка запуска контейнера${NC}"
        exit 1
    fi
}

# Универсальная функция для остановки контейнера
stop_service() {
    echo -e "${YELLOW}🛑 Остановка контейнера...${NC}"
    
    if docker stop $CONTAINER_STATS_NAME; then
        echo -e "${GREEN}✅ Контейнер остановлен${NC}"
    else
        echo -e "${RED}❌ Ошибка остановки контейнера${NC}"
        exit 1
    fi
}

# Универсальная функция для перезапуска контейнера
restart_service() {
    echo -e "${YELLOW}🔄 Перезапуск контейнера...${NC}"
    
    if docker restart $CONTAINER_STATS_NAME; then
        echo -e "${GREEN}✅ Контейнер перезапущен${NC}"
        echo -e "${YELLOW}💡 Для запуска приложения используйте: $COMMAND_NAME app-start${NC}"
    else
        echo -e "${RED}❌ Ошибка перезапуска контейнера${NC}"
        exit 1
    fi
}

# Функция для запуска приложения в контейнере
start_app() {
    echo -e "${YELLOW}📱 Запуск приложения в контейнере...${NC}"
    
    # Проверяем, что контейнер запущен
    if ! docker ps --filter "name=$CONTAINER_STATS_NAME" --filter "status=running" | grep -q "$CONTAINER_STATS_NAME"; then
        echo -e "${RED}❌ Контейнер не запущен!${NC}"
        echo -e "${YELLOW}💡 Сначала запустите контейнер: $COMMAND_NAME start${NC}"
        exit 1
    fi
    
    # Проверяем, работает ли приложение уже
    if docker exec $CONTAINER_STATS_NAME pgrep -f "python3 $MAIN_SCRIPT" > /dev/null; then
        echo -e "${GREEN}✅ Приложение уже работает${NC}"
    else
        # Запускаем main.py
        if docker exec -d $CONTAINER_STATS_NAME python3 $MAIN_SCRIPT; then
            echo -e "${GREEN}✅ Приложение запущено${NC}"
        else
            echo -e "${RED}❌ Ошибка запуска приложения${NC}"
            echo -e "${YELLOW}💡 Проверьте логи: $COMMAND_NAME logs${NC}"
            exit 1
        fi
    fi
}

# Функция для остановки приложения в контейнере
stop_app() {
    echo -e "${YELLOW}🛑 Остановка приложения в контейнере...${NC}"
    
    # Проверяем, что контейнер запущен
    if ! docker ps --filter "name=$CONTAINER_STATS_NAME" --filter "status=running" | grep -q "$CONTAINER_STATS_NAME"; then
        echo -e "${RED}❌ Контейнер не запущен!${NC}"
        exit 1
    fi
    
    # Проверяем, есть ли процессы приложения
    PROCESSES=$(docker exec $CONTAINER_STATS_NAME pgrep -f "python3 $MAIN_SCRIPT")
    if [ -z "$PROCESSES" ]; then
        echo -e "${YELLOW}⚠️ Приложение не запущено${NC}"
        return 0
    fi
    
    echo -e "${YELLOW}📋 Найдены процессы: $PROCESSES${NC}"
    
    # Сначала пытаемся мягко остановить (SIGTERM)
    echo -e "${YELLOW}🔄 Отправка SIGTERM...${NC}"
    docker exec $CONTAINER_STATS_NAME pkill -f "python3 $MAIN_SCRIPT"
    
    # Ждем 3 секунды
    sleep 3
    
    # Проверяем, остановились ли процессы
    REMAINING=$(docker exec $CONTAINER_STATS_NAME pgrep -f "python3 $MAIN_SCRIPT")
    if [ -n "$REMAINING" ]; then
        echo -e "${YELLOW}⚠️ Процессы не остановились, отправка SIGKILL...${NC}"
        docker exec $CONTAINER_STATS_NAME pkill -9 -f "python3 $MAIN_SCRIPT"
        sleep 1
    fi
    
    # Финальная проверка
    FINAL_CHECK=$(docker exec $CONTAINER_STATS_NAME pgrep -f "python3 $MAIN_SCRIPT")
    if [ -z "$FINAL_CHECK" ]; then
        echo -e "${GREEN}✅ Приложение остановлено${NC}"
    else
        echo -e "${RED}❌ Не удалось остановить приложение${NC}"
        echo -e "${YELLOW}💡 Оставшиеся процессы: $FINAL_CHECK${NC}"
        exit 1
    fi
}

# Функция для перезапуска приложения в контейнере
restart_app() {
    echo -e "${YELLOW}🔄 Перезапуск приложения в контейнере...${NC}"
    
    # Проверяем, что контейнер запущен
    if ! docker ps --filter "name=$CONTAINER_STATS_NAME" --filter "status=running" | grep -q "$CONTAINER_STATS_NAME"; then
        echo -e "${RED}❌ Контейнер не запущен!${NC}"
        echo -e "${YELLOW}💡 Сначала запустите контейнер: $COMMAND_NAME start${NC}"
        exit 1
    fi
    
    # Убиваем все процессы main.py в контейнере
    echo -e "${YELLOW}🛑 Остановка приложения...${NC}"
    docker exec $CONTAINER_STATS_NAME pkill -f "python3 $MAIN_SCRIPT" 2>/dev/null || true
    
    # Ждем немного, чтобы процессы завершились
    sleep 2
    
    # Запускаем main.py заново
    echo -e "${YELLOW}📱 Запуск приложения...${NC}"
    if docker exec -d $CONTAINER_STATS_NAME python3 $MAIN_SCRIPT; then
        echo -e "${GREEN}✅ Приложение перезапущено${NC}"
    else
        echo -e "${RED}❌ Ошибка перезапуска приложения${NC}"
        echo -e "${YELLOW}💡 Проверьте логи: $COMMAND_NAME logs${NC}"
        exit 1
    fi
}

# Функция для просмотра логов приложения (файл)
show_app_logs() {
    echo -e "${YELLOW}📋 Просмотр логов приложения...${NC}"
    
    # Проверяем, что контейнер запущен
    if ! docker ps --filter "name=$CONTAINER_STATS_NAME" --filter "status=running" | grep -q "$CONTAINER_STATS_NAME"; then
        echo -e "${RED}❌ Контейнер не запущен!${NC}"
        exit 1
    fi
    
    # Получаем количество строк (по умолчанию 10)
    LINES=${1:-10}
    
    # Проверяем, что это число
    if ! [[ "$LINES" =~ ^[0-9]+$ ]]; then
        echo -e "${RED}❌ Количество строк должно быть числом!${NC}"
        echo -e "${YELLOW}💡 Использование: $COMMAND_NAME app-logs [количество_строк]${NC}"
        exit 1
    fi
    
    echo -e "${YELLOW}📋 Показываю последние $LINES строк логов...${NC}"
    
    # Показываем логи приложения из файла
    docker exec $CONTAINER_STATS_NAME tail -f -n $LINES /workspace/logs/bot.log
}

# Универсальная функция для просмотра логов контейнера
show_logs() {
    docker logs -f $CONTAINER_STATS_NAME
}

# Универсальная функция для проверки статуса
show_status() {
    echo -e "${BLUE}📊 Статус контейнера:${NC}"
    docker ps --filter "name=$CONTAINER_STATS_NAME"
    
    echo -e "\n${BLUE}📱 Статус приложения:${NC}"
    if docker exec $CONTAINER_STATS_NAME pgrep -f "python3 $MAIN_SCRIPT" > /dev/null 2>&1; then
        # Получаем информацию о процессе
        PROCESS_INFO=$(docker exec $CONTAINER_STATS_NAME ps aux | grep "python3 $MAIN_SCRIPT" | grep -v grep)
        if [ -n "$PROCESS_INFO" ]; then
            PID=$(echo "$PROCESS_INFO" | awk '{print $2}')
            CPU=$(echo "$PROCESS_INFO" | awk '{print $3}')
            MEM=$(echo "$PROCESS_INFO" | awk '{print $4}')
            TIME=$(echo "$PROCESS_INFO" | awk '{print $10}')
            echo -e "${GREEN}✅ Приложение работает${NC}"
            echo -e "   PID: $PID | CPU: $CPU% | MEM: $MEM% | TIME: $TIME"
        else
            echo -e "${YELLOW}⚠️ Процесс найден, но информация недоступна${NC}"
        fi
    else
        echo -e "${RED}❌ Приложение не запущено${NC}"
        echo -e "${YELLOW}💡 Запустите: $COMMAND_NAME start${NC}"
    fi
    
    echo -e "\n${BLUE}📈 Использование ресурсов контейнера:${NC}"
    docker stats --no-stream $CONTAINER_STATS_NAME 2>/dev/null || echo "Контейнер не запущен"
}

# Функция для работы с БД
manage_app_database() {
    echo -e "${YELLOW}🗄 Работа с базой данных...${NC}"
    
    # Проверяем, что контейнер запущен
    if ! docker ps --filter "name=$CONTAINER_STATS_NAME" --filter "status=running" | grep -q "$CONTAINER_STATS_NAME"; then
        echo -e "${YELLOW}⚠️ Контейнер не запущен, запускаю...${NC}"
        docker start $CONTAINER_STATS_NAME
    fi
    
    # Запускаем database_manager.py с переданными параметрами
    shift  # Убираем первый аргумент (app-db)
    docker exec $CONTAINER_STATS_NAME python3 $DATABASE_MANAGER_SCRIPT "$@"
}

# Универсальная функция для запуска shell в контейнере
open_shell() {
    echo -e "${YELLOW}🐚 Запуск shell в контейнере...${NC}"
    
    # Проверяем, что контейнер запущен
    if ! docker ps --filter "name=$CONTAINER_STATS_NAME" --filter "status=running" | grep -q "$CONTAINER_STATS_NAME"; then
        echo -e "${YELLOW}⚠️ Контейнер не запущен, запускаю...${NC}"
        docker start $CONTAINER_STATS_NAME
    fi
    
    # Запускаем bash в контейнере
    docker exec -it $CONTAINER_STATS_NAME bash
}

# Функция для обновления приложения
update_app() {
    echo -e "${YELLOW}🔄 Обновление $COMMAND_DESCRIPTION...${NC}"
    
    # Для обновления все-таки нужен доступ к файлам проекта
    if ! find_project_dir; then
        echo -e "${RED}❌ Проект не найден!${NC}"
        echo -e "${YELLOW}💡 Убедитесь, что вы находитесь в папке проекта или установите переменную CORENESS_PROJECT_DIR${NC}"
        exit 1
    fi
    
    # Запускаем core_updater.py
    python3 "$PROJECT_DIR/$CORE_UPDATER_SCRIPT"
}

# Универсальная функция для экстренной остановки
emergency_stop() {
    echo -e "${RED}💀 KILL ALL DOCKER${NC}"
    echo -e "${YELLOW}⚠️ Убиваю ВСЕ Docker процессы!${NC}"
    echo ""
    
    # Остановить все контейнеры
    echo -e "${YELLOW}🛑 Останавливаю контейнеры...${NC}"
    docker stop $(docker ps -q) 2>/dev/null || echo "Нет контейнеров"
    
    # Удалить все контейнеры
    echo -e "${YELLOW}🗑 Удаляю контейнеры...${NC}"
    docker rm $(docker ps -aq) 2>/dev/null || echo "Нет контейнеров"
    
    # Остановить Docker сервис
    echo -e "${YELLOW}⏹ Останавливаю Docker...${NC}"
    sudo systemctl stop docker 2>/dev/null || echo "Docker остановлен"
    
    # Убить все процессы Docker
    echo -e "${YELLOW}💀 Убиваю процессы...${NC}"
    sudo pkill -f docker 2>/dev/null || echo "Нет процессов"
    
    echo -e "${GREEN}✅ ВСЕ УБИТО!${NC}"
    echo -e "${YELLOW}💡 Запуск: sudo systemctl start docker${NC}"
}

# Универсальная функция для показа справки
show_help() {
    echo -e "${BLUE}$COMMAND_DESCRIPTION${NC}"
    echo ""
    echo "Использование: $COMMAND_NAME [команда] [параметры]"
    echo ""
    echo "Команды:"
    echo "  install         Установить глобальные команды"
    echo "  start           Запустить контейнер"
    echo "  stop            Остановить контейнер"
    echo "  restart         Перезапустить контейнер"
    echo "  app-start       Запустить приложение в контейнере"
    echo "  app-stop        Остановить приложение в контейнере"
    echo "  app-restart     Перезапустить приложение в контейнере"
    echo "  app-update      Обновить приложение"
    echo "  app-db          Работа с базой данных"
    echo "  app-logs [N]    Показать логи приложения (файл, N строк)"
    echo "  logs            Показать логи контейнера"
    echo "  status          Показать статус контейнера"
    echo "  shell           Запустить shell в контейнере"
    echo "  kill            💀 Убить ВСЕ Docker процессы"
    echo "  help            Показать эту справку"
    echo ""
    echo "Примеры:"
    echo "  $COMMAND_NAME start                             # Запустить контейнер"
    echo "  $COMMAND_NAME app-start                         # Запустить приложение"
    echo "  $COMMAND_NAME app-logs 50                       # Логи приложения (50 строк)"
    echo "  $COMMAND_NAME app-db --all --migrate            # Миграция БД"
}

# Основная логика
main() {
    # Для команды install не проверяем Docker
    if [ "$1" = "install" ]; then
        install_commands
        exit 0
    fi
    
    # Для остальных команд проверяем Docker
    check_docker
    
    case "${1:-help}" in
        start)
            start_service
            ;;
        stop)
            stop_service
            ;;
        restart)
            restart_service
            ;;
        app-start)
            start_app
            ;;
        app-stop)
            stop_app
            ;;
        app-restart)
            restart_app
            ;;
        app-logs)
            show_app_logs "$2"
            ;;
        logs)
            show_logs
            ;;
        status)
            show_status
            ;;
        app-db)
            manage_app_database "$@"
            ;;
        shell)
            open_shell
            ;;
        app-update)
            update_app
            ;;
        kill)
            emergency_stop
            ;;
        help|--help|-h)
            show_help
            ;;
        *)
            echo -e "${RED}❌ Неизвестная команда: $1${NC}"
            echo ""
            show_help
            exit 1
            ;;
    esac
}

# Запуск
main "$@"
